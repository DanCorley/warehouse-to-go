[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "warehouse-to-go"
version = "0.2.0"
description = "Tool to create local DuckDB representations of data warehouse sources from dbt projects"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb==1.5.5",
    "pyarrow>=17.0.0",
    "snowflake-connector-python[pandas]==4.2.0",
    "cryptography==44.0.2",
    "PyYAML==6.0.1",
    "typer>=0.15.2",
    "click>=8.0,<9.0",
    "rich==14.0.0"
]

[project.optional-dependencies]
dev = [
    "pytest==9.1.1",
    "dbt-core==1.12.3",
    "dbt-duckdb==1.11.0",
    "dbt-postgres==1.11.0",
    "dbt-snowflake==1.12.0",
    "dbt-polyglot==0.2.0"
]

[project.scripts]
warehouse-to-go = "warehouse_to_go.cli:app"

# Adapter discovery. Each adapter module is discovered through this
# entry-point group; add one module path per dialect.
[project.entry-points."warehouse_to_go.adapters"]
snowflake = "warehouse_to_go.warehouse.snowflake_adapter"
postgres = "warehouse_to_go.warehouse.postgres_adapter"
bigquery = "warehouse_to_go.warehouse.bigquery_adapter"

[tool.setuptools.packages.find]
where = ["."]
include = ["warehouse_to_go*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv.sources]
warehouse-to-go = { workspace = true }

[dependency-groups]
dev = [
    "warehouse-to-go",
]
